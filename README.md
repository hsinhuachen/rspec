# Rspec

## Gemfile
    group :development, :test do
        gem 'byebug', platform: :mri
        gem 'rspec-rails'
        gem 'factory_girl_rails'
        gem 'faker'
    end

## Rspec install
    $ bin/rails g rspec:install

## Run
    $ rspec

